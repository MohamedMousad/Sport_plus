@using SportPlus.DAL.Enums
@model teamInput
@{
    ViewData["Title"] = "Standing";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<body>
    <div class="hero overlay">
        <img src="~/images/bg_3.jpg" />
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-5 mx-auto text-center">
                    <h1 class="text-white">Standing Form</h1>
                    <p></p>
                </div>
            </div>
        </div>
    </div>
    <!-- Form to submit to TeamStatsSubmit -->
    <form asp-action="Standings" asp-controller="Home" method="post">
        <!-- League Dropdown -->
        <div class="mb-4">
            <label for="League" class="form-label">League</label>
            <select asp-for="League" id="League" class="form-select" required>
                <option value="">Select League</option>
                @foreach (var league in Enum.GetValues(typeof(SportPlus.DAL.Enums.League)))
                {
                    <option value="@league">@league</option>
                }
            </select>
        </div>

        <!-- Season Dropdown -->
        <div class="mb-4">
            <label for="Season" class="form-label">Season</label>
            <select asp-for="Season" id="Season" class="form-select" required>
                <option value="">Select Season</option>
                <option value="2020">2020</option>
                <option value="2021">2021</option>
                <option value="2022">2022</option>
            </select>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>


</body>