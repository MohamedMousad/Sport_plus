@using SportPlus.DAL.Enums
@model teamInput
@{
    ViewData["Title"] = "Team Statistics Input";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <h2>Input Team Statistics</h2>
    <!-- Form to submit to TeamStatsSubmit -->
    <form asp-action="TeamStats" asp-controller="Home" method="post">
        <!-- Team Dropdown -->
        <div class="mb-4">
            <label for="Team" class="form-label">Choose Team</label>
            <select asp-for="Team" id="Team" class="form-select" required>
                <option value="">Select Favorite Team</option>
                @foreach (var team in Enum.GetValues(typeof(FavouriteTeam)))
                {
                    <option value="@team">@team</option>
                }
            </select>
        </div>

        <!-- League Dropdown -->
        <div class="mb-4">
            <label for="League" class="form-label">League</label>
            <select asp-for="League" id="League" class="form-select" required>
                <option value="">Select League</option>
                @foreach (var league in Enum.GetValues(typeof(SportPlus.DAL.Enums.League)))
                {
                    <option value="@league">@league</option>
                }
            </select>
        </div>

        <!-- Season Dropdown -->
        <div class="mb-4">
            <label for="Season" class="form-label">Season</label>
            <select asp-for="Season" id="Season" class="form-select" required>
                <option value="">Select Season</option>
                <option value="2020">2020</option>
                <option value="2021">2021</option>
                <option value="2022">2022</option>
            </select>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</div>
